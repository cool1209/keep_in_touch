export const getDialogs = (state) => (
  state.dialogs.dialogs
);

export const getTotalDialogs = (state) => (
  state.dialogs.totalDialogs
);

export const getIsContactSelected = (state) => (
  state.dialogs.isContactSelected
);
